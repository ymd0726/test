# jde_mak 集計表 構造実測（Phase 0）

- スプレッドシート: `jde集計`（ID: `1oEK8JCJg2NcseWmnxLtfNFCe5A7XGJ_DJSj7c2EW1sg`）
- 実測方法: GitHub Actions「集計表 構造ダンプ（cr入稿くん Phase 0）」を Run workflow
  （`scripts/cr-submit/inspect_sheet.mjs` / 読み取り専用 / 既存 `GOOGLE_SERVICE_ACCOUNT_JSON` シークレット使用）

## 参考: jdem meta_total の既知構造（CR親子分類仕様書より）

| 項目 | 値 |
|---|---|
| `集計外CR→`マーカー | 0行目（表示1行目）col 1894 |
| 左ブロック（親/単独CR） | 約25列/ブロック |
| 右ブロック（子CR） | 約19列/ブロック |
| クリエイティブID行 | 6行目（案件により7〜8行目。ssh等） |
| メモ列 | 各ブロック末尾（0行目=`メモ`）。2行目=親子分類プルダウン(data!Q2:Q5) / 3行目=停止判定式 / 4行目=強制停止チェック |
| cr00テンプレ | 同一IDの重複ブロックあり（jdem: cr00×3） |

## jde_mak 実測結果（2026-07-03 Actions実測・run 28647732284）

| 項目 | 実測値 |
|---|---|
| 対象タブ名 | `kk_mak`（sheetId 1843459739）。kk_kouも同構造 |
| `集計外CR→`マーカー列 | **存在しない**（jdem meta_total固有の目印。jdeには無い） |
| クリエイティブID行 | **6行目** |
| ブロック数（メモ列数） | 99（ヘッダー実使用 1595列） |
| 親用cr00ブロック（左端のcr00） | cols 77-97（`BZ:CT`）・**幅21列** |
| 子用cr00ブロック（右端のcr00） | cols 859-873（`AGB:AGP`）・**幅15列** |
| 列グループ化 | 深度1のグループ多数（collapsed）。ブロックごとにグループあり→GASで再作成対応済み |
| kk_kou参考 | 親cr00: cols 27-50 幅24 / 子cr00: cols 755-769 幅15 |
| 直近入稿広告名の慣習 | `jde_mak_cr84_バナー動画_ブライダル_プレ花嫁`（フル名称）— Meta APIで実測済み(2026-07-02) |
| Meta広告アカウント | `n22_jdes` = `1533513563939156`（jde_mak/jde_kou同居） |
| mak本体キャンペーン/広告セット | campaign `120227310484980183` / adset `120246843077960183`（cr81/82/84の直近入稿先） |

### 実測を受けた設計変更（適用済み）
- マーカーが無いため、ゾーン判定は「**左端のcr00=親テンプレ／右端のcr00=子テンプレ**」方式に変更
  （マーカーがあるシートでは従来どおりマーカー基準。`submitCreative_common.gs`）
- dryRun検証時は、親が col98（BZ隣）、子が col874（AGP隣）に入る計画になることを確認する
